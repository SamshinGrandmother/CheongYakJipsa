spring:
  application:
    name: CheongyakJipsa
  jpa:
    hibernate:
      ddl-auto: none
  messages:
    basename: messages
server:
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force-response: true
      force-request: true
